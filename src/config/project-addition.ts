import type { ProjectAdditionConfig } from "@/types/project-addition";

export const projectAdditionConfig: ProjectAdditionConfig = {
  id: "2026-03-merge-sort",
  title: "What's New",
  description: "Merge Sort is now available.",
  enabled: true,
};
